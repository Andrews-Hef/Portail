{% extends 'base.html.twig' %}

{% block title %}Resultat correspondant{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="">
    <br><br><br><br>
    <h3> 
        Recherche  
        <br>
        <span> {{ researchResults|length }} résultat</span>
    </h3>
    {% for item in researchResults %}
        <div class= "card-columms text-center mx-2"  style ="display: inline-block">
            <div class="card bg-dark" style="width: 18rem;">
                <h4 class="text-light">{{ item.title }}</h4> 
                <img style="width:100%; height:100%;" src="https://image.tmdb.org/t/p/original{{item.poster_path}}" > 
                <div class="card-footer">
                    <small class="text-muted">
                        <a class="text-light" href="{{ path('cinema.MovieView',{id:item.id,idcine:idCine}) }}">cliquez ici </a>
                    </small>
                </div>
            </div>
        </div>

    {% endfor %}

{% endblock %}